/*
* Syntax: character = str.charAt(index)
* If no index is provided to charAt(),
* the default is 0.
*
* */

var message = 'Hello World';

console.log (
    message.charAt(),   // H <-- default index = 0
    message.charAt(1),   // e
);